# Claude Code local config (keep on disk, never publish)
.claude/

# Editor / atomic-write temp files
*.tmp.*

# Local visual-design scratchpad
icon-concepts.html

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# Bluesky bot local output, dependencies, and per-mention retry state
bot/node_modules/
bot/state.json
bot-output.jpg
